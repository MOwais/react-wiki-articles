import SearchBar from "./SearchBar";
import ArticlesList from "./ArticlesList";

const Articles = () => {
  return (
    <div>
      <SearchBar />
      <ArticlesList />
    </div>
  );
};

export default Articles;
